import 'package:flutter/material.dart';

class AddEditTaskView extends StatelessWidget {
  const AddEditTaskView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('add edit task page')]);
  }
}
